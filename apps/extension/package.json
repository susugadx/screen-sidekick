{
  "name": "screen-sidekick-extension",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test tests/*.test.mjs"
  },
  "devDependencies": {
    "@types/chrome": "0.1.42",
    "jsdom": "^29.1.1",
    "typescript": "6.0.3"
  }
}
