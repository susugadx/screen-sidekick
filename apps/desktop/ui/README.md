# Desktop UI

Leptos CSR UI for the desktop shell.

The UI may render previews, collect user confirmation, and call typed Tauri
commands. It must not own ScreenContext schema rules, danger detection, masking,
or prompt generation.

Phase 0-B only displays bridge URL/token/status and copy controls.
